项目已成功在本地启动。

1.  **依赖安装**: 已完成 (`npm install`).
2.  **前端服务**: 运行在 [http://localhost:7200](http://localhost:7200).
3.  **后端服务**: 运行在 [http://localhost:3000](http://localhost:3000).

注意：
-   前端代码已配置为在本地开发时直接连接 `http://localhost:3000` 的 API。
-   如果使用图片生成功能，请确保在前端设置中配置了 API Key，并保持后端服务运行。
-   预览页面中出现的 `net::ERR_CONNECTION_REFUSED` 错误是在后端启动之前发生的，刷新页面后应正常。